import { Text, View } from 'react-native'
import React, { Component } from 'react'
import AppTheme from '../../../helper/AppTheme'

export default class Icon extends Component {
  render() {
    return (
      <View>
        <Text style={{color:AppTheme.APPCOLOR.BLACK}}>Idsfsdf</Text>
      </View>
    )
  }
}